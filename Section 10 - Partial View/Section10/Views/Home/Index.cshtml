
<div class="container">
    <h1>Cities List</h1>

    <h2>From Main View</h2>

    <table class="city-grid">
        <thead>
            <tr>
                <th>City ID</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @if (ViewData.ContainsKey("Cities") && ViewData["Cities"] != null)
            {
                foreach (var city in (List<City>)ViewData["Cities"])
                {
                    <tr>
                        <td>@city.Id</td>
                        <td>@city.Name</td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="2" style="text-align:center;">No cities available.</td>
                </tr>
            }
        </tbody>
    </table>
    <style>
        .city-grid {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1rem;
        }

            .city-grid th, .city-grid td {
                border: 1px solid #ccc;
                padding: 0.5rem;
                text-align: left;
            }

            .city-grid th {
                background-color: #f2f2f2;
            }
    </style>

    <h2>Tag</h2>
    <partial name="_CityGridPartialView" />

    <h2>Partial Helper</h2>
    @Html.Partial("_CityGridPartialView")

    <h2>Partial Helper Async</h2>
    @await Html.PartialAsync("_CityGridPartialView")

    <h2>Render Partial Helper</h2>
    @{
        Html.RenderPartial("_CityGridPartialView");
}

    <h2> Render Partial Helper Async</h2>
    @{
        await Html.RenderPartialAsync("_CityGridPartialView");
    }
</div>
